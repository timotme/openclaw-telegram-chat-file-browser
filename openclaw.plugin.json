{
  "id": "openclaw-telegram-file-browser",
  "name": "Telegram File Browser",
  "description": "File browser for OpenClaw workspace via Telegram inline buttons",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "maxButtonsPerRow": {
        "type": "integer",
        "minimum": 1,
        "maximum": 4,
        "default": 2,
        "description": "Number of buttons per row in the file browser UI (1-4)"
      },
      "maxButtonsTotal": {
        "type": "integer",
        "minimum": 10,
        "maximum": 100,
        "default": 40,
        "description": "Maximum total number of file/folder buttons to display (10-100)"
      },
      "maxTextPreview": {
        "type": "integer",
        "minimum": 500,
        "maximum": 10000,
        "default": 2500,
        "description": "Maximum bytes to display per text file chunk (500-10000)"
      }
    }
  }
}
